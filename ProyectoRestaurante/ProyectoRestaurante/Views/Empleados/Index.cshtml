@model List<Empleado>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewData["Title"] = "Empleados";
}


<br />
<div class="container table-info">
    <div class="row">
        <div class="col-8 bg-primary fw-bold">
            <h2>Empleados</h2>
        </div>

        <div class="col-4 bg-primary fw-bold text-right">
            <a asp-controller="Empleados" asp-action="Create" class="btn btn-primary text-white">Crear</a>
        </div>
    </div>
    <div class="row">
        <div class="col-2 bg-primary fw-bold text-center">
            Cedula
        </div>
        <div class="col-3 bg-primary fw-bold text-center">
            Nombre Completo
        </div>
        <div class="col-2 bg-primary fw-bold text-center">
            Correo
        </div>
        <div class="col-2 bg-primary fw-bold text-center">
            Rol
        </div>
        <div class="col-3 bg-primary fw-bold text-center">
            Acción
        </div>

    </div>
    @foreach (Empleado empleado in Model)
    {
        <div class="row  text-center bg-dark" id="@empleado.CedulaEmpleado">
            <div class="col-2">
                @empleado.CedulaEmpleado
            </div>

            <div class="col-3">
                @empleado.Nombre @empleado.Apellido1 @empleado.Apellido2
            </div>

            <div class="col-2">
                @empleado.Correo
            </div>


            <div class="col-2">
                @empleado.Rol.Nombre
            </div>

            <div class="col-3">
                <a id="editar" asp-controller="Empleados" asp-action="Upsert" asp-route-CedulaEmpleado="@empleado.CedulaEmpleado" class="btn btn-sm btn-success text-white">
                    <i class="fas fa-edit"></i>
                </a>
                <a id="borrar" onclick="Borrar('@empleado.CedulaEmpleado');" class="btn btn-sm btn-danger text-white">
                    <i class="fas fa-trash-alt"></i>
                </a>
            </div>
        </div>
    }

</div>


<script>
    function Borrar(id) {
        swal
            ({

                title: "Borrar Empleado",
                text: "¿Está seguro que desea borrar el registro?",
                icon: "warning",
                buttons: true,
                dangerMode: true
            }).then
            (
                (borrarlo) => {
                    if (borrarlo) {
                        $.ajax
                            ({
                                type: "DELETE",
                                url: "/empleados/borrar/?CedulaEmpleado=" + id,
                                success: function (data) {
                                    if (data.success) {
                                        toastr.success("El registro ha sido borrado permanentemente.");
                                        $("#" + id).remove();
                                    }
                                    else {
                                        toastr.error(data.message);
                                    }
                                }
                            });
                    }
                }
            );
    }
</script>